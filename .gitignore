__pycache__/
.pytest_cache/
*.py[cod]
.venv/
.ipynb_checkpoints/

# Run outputs are not committed; regenerate with experiments/toy_smc_cfg.py.
results/
*.pt
*.ckpt

# Persisted environment and caches, if CLG_PERSIST points inside the repository
# rather than at its parent. See vlab_env.sh.
envs/
hf/
pip-cache/
conda/
git/

# Reference image sets for FID are large and not ours to redistribute.
# Prompt lists under data/prompts/ are small and should be committed: they
# define the experiment. Nothing in this repository is trained, so there is no
# training data to store here.
data/reference/

# The paper PDF is not ours to redistribute; the write-up in docs/ is tracked.
docs/*.pdf
